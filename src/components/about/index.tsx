import React from 'react'
import { sections } from '../../utils/index'

export const About = () => (
  <section id={sections.about.section} style={{height: '1000px'}}>
    <div>About Page</div>
  </section>
)
